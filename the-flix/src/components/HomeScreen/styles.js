import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  homeScreen: {
    backgroundColor: '#111',
    color: '#fff',
    height: '100vh',
  }
  
}));

export default useStyles;